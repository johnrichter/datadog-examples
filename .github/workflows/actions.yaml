name: "Datadog Examples Workflow"
run-name: "${{ github.actor }} is testing out GitHub Actions"
on:
  push:
    branches:
      - "main"
jobs:
  test:
    runs-on: "ubuntu-latest"
    steps:
      - run: "echo Hello world!"
